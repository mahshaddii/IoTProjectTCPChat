# IoT Project TCP/IP Chat

## Usage:
 1. Run this file in a terminal to start the server.
    ```bash
    python3 server.py
    ```
 3. Open a new terminal and run the client.
    ```bash
    python3 client.py
    ```
 5. Type messages in each terminal to simulate a chat between client and server.
